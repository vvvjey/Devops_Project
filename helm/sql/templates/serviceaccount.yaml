{{- if .Values.serviceAccount.create | default false }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "sql.serviceAccountName" . }}
  labels:
    {{- include "sql.labels" . | nindent 4 }}
{{- end }}
